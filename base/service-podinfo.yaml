apiVersion: v1
kind: Service
metadata:
  name: kargo-podinfo
spec:
  type: NodePort
  selector:
    app: kargo-podinfo
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 80
    nodePort: placeholder
